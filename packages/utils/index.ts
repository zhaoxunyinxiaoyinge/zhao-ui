import {isAarray,isNumber,isObject} from "./common";
import {rowContextKey} from "./tokens";
import {createNamespace} from "./createBem";
export  {
  createNamespace,
  rowContextKey,
  isAarray,
  isNumber,
  isObject
}